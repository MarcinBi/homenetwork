# Stack: vpn

Router currently provides WireGuard. This folder documents VPN-related choices and future plans.

On the Debian server:
- /srv/docker/wireguard/config exists
- /opt/stacks/vpn currently contains README/.env only (no compose yet)
