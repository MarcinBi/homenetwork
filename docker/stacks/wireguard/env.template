# Public hostname or IP for WireGuard clients
WG_HOST=wg.example.com

# Password hash for the admin UI
# Generate with:
# docker run --rm ghcr.io/wg-easy/wg-easy wgpw 'your-password'
PASSWORD_HASH=change-me

# Persistent data directory on NAS
WG_DATA_DIR=/volume1/docker/wireguard

