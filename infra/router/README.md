# Router

Document router config needed to rebuild network access:
- LAN subnet(s), gateway, VLANs
- DHCP ranges + reservations
- DNS forwarding behavior
- WireGuard (if on router)
- Port forwards
- Backup/restore process
